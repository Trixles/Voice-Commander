# THE SCRUB — Implementation Plan (2026-09-11)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans
> (Tyler chose inline execution, this session). Steps use checkbox (`- [ ]`)
> syntax for tracking. Commits are GATED: plain-English summary → Tyler's
> explicit yes → commit. Deploy likewise gated.

**Goal:** Three amputations on the clean tree: (1) the Vosk recognizer arm,
(2) the openWakeWord audio-wake arm, (3) celery_man out of shipped defaults.
Text wake + whisper become the only paths; existing user configs keep working
untouched.

**Architecture:** Pure removal + seam simplification. `make_recognizer_factory()`
loses its backend branch (whisper only). `run_listener()` loses the
`wake_engine_factory` injection and every `audio_wake_unconfirmed` rung.
Config keys are deleted from `--emit-defaults`; stale keys in existing
configs ride along silently (load_config never prunes — by design, keep it).

**Tech stack:** Python 3.14, pytest, bash (install.sh). No new deps — only
removals.

## Context

Tyler approved the scrub s39 after the phantom census (~50 silent false
fires/day over media) led to the ruling: OWW IS OUT of VC, text wake is THE
wake path. Vosk was already non-default in practice (live config runs
whisper) and its removal was a standing priority. Celery_man leaves defaults
per the s37 ruling (action machinery stays so existing configs dispatch).

Decisions locked with Tyler (2026-09-11):
- **`recognizer_backend` key: DELETE entirely** (not flip). Getter, fallback,
  emit-defaults entry all go.
- **`fuzzy_check()`: DELETE** (+ its 7 tests + `wake_confirm`). Dead once the
  audio confirm rung goes; text wake is exact whole-word.
- **README: surgical lie-fixing pass now**; full rewrite stays a separate
  2.0 session.
- **Execution: inline, this session**, commit-gated per arm.

Key facts from exploration (verified against source):
- `install.sh:225` does `rm -rf "${VENV_DIR}"` every run → the post-scrub
  deploy auto-purges vosk/openwakeword/scipy/etc. from the venv. No manual
  uninstall step.
- `requirements.txt` lines 9–23: comment block + `tqdm`/`scipy`/`requests`/
  `scikit-learn` exist ONLY for openwakeword (zero import sites in
  core/, voice_commander.py, tests/). `onnxruntime`+`numpy` stay
  (`core/vad.py` — the segment-dispatch Silero VAD, which is LOAD-BEARING
  and completely separate from oww's internal speech-gate VAD).
- `fuzzy_check` has exactly one production call site (`listener.py:680`);
  `wake_confirm` exactly one consumer (`listener.py:357`).
- `load_config()` merge (`commands.py:402-424`) is add-only by name — user
  configs keep celery_man + stale keys verbatim. NO config prune anywhere.
- `_SYSTEM_COMMAND_ORDER` in `core/settings/helpers.py:161` KEEPS
  `celery_man` — otherwise every existing config with the row triggers the
  once-per-process stderr warning and the row sorts to the bottom.
- Working tree is clean; repo `wake_dump.py` has `keep=50` (the 500 was
  reverted pre-commit; deployed copy diverges harmlessly until deploy —
  moot, the file is deleted by this scrub).
- `main` is 1 commit ahead of origin (`3b5097f` auto-pause). Push is a
  separate, gated decision — not part of this plan.

## Global constraints

- **Gauntlet** (must pass before every commit):
  `python -m pytest -q && python -m compileall -q core && python -m core.commands --emit-defaults >/dev/null 2>/dev/null && echo "✅ green"`
  (never merge emit-defaults stderr into stdout — it pollutes the JSON).
- `core/commands.py` and `core/aliases.py` stay importable WITHOUT PySide6.
- Commits and deploy each need Tyler's explicit yes, plain-English summary
  first, no raw diffs unprompted.
- Never touch `~/.local/share/voice-commander/app/` directly — deploy only
  via `./install.sh`, then byte-verify.
- ARCHITECTURE.md updates land in the SAME commit as the code they describe.
- Don't touch: `core/vad.py`, `whisper_*`, auto-pause/wake-window logic,
  celery action machinery, `wake_words`/`BAKED_IN_WAKE_WORDS` plumbing,
  `per_segment_finals` seam (test harness relies on the False default).
- Historical docs stay historical: CHANGELOG old entries,
  `docs/superpowers/{plans,specs}/2026-07-28*` and `2026-08-26*`,
  `.claude/specs/`. No retro-editing.

---

### Task 0: Plan file into the repo

**Files:** Create `docs/superpowers/plans/2026-09-11-the-scrub.md`

- [ ] **Step 1:** Copy this plan into
  `docs/superpowers/plans/2026-09-11-the-scrub.md` (repo convention:
  date-slug, plans/ dir). It rides along in the Task 1 commit.

---

### Task 1: Celery-defaults cut (warm-up arm)

**Files:**
- Modify: `core/commands.py:302-311` (delete block)
- Modify: `tests/test_celery_man.py:153-159` (flip test)

**Interfaces:** `_default_system_commands()` no longer returns a
`celery_man` entry. `ACTION_REGISTRY["celery_man"]`, notification template,
`helpers.py` display-name map AND `_SYSTEM_COMMAND_ORDER` entry all STAY.

- [ ] **Step 1: Flip the defaults test** — replace
  `test_default_system_command_present_and_shaped` (tests/test_celery_man.py:153-159) with:

```python
def test_celery_man_not_in_default_system_commands():
    # Ruling s37: the easter egg leaves shipped defaults. Existing configs
    # keep their row (merge-on-load only ADDS by name, never prunes) and it
    # still dispatches because the action machinery stays registered.
    names = [c["name"] for c in commands._default_system_commands()]
    assert "celery_man" not in names
```

  Keep `test_is_last_in_system_command_order` unchanged (the order entry
  stays so existing configs sort silently).

- [ ] **Step 2: Run it, watch it fail** —
  `python -m pytest tests/test_celery_man.py -q` → the new test FAILS
  (entry still present).

- [ ] **Step 3: Delete the defaults entry** — remove the whole
  `commands.append({...})` block at `core/commands.py:302-311` (the
  `"name": "celery_man"` block, last before `return commands`), including
  its comment (which is stale anyway — claims wake-word muting that was
  replaced by the MPRIS check).

- [ ] **Step 4: Full gauntlet** → green.

- [ ] **Step 5: COMMIT GATE** — summarize to Tyler; on yes:
  `git add -A && git commit` — message like
  `feat: drop celery_man from shipped defaults (action machinery stays)`.
  Includes Task 0's plan file.

---

### Task 2: Vosk arm

**Files:**
- Modify: `core/recognizer.py` (delete `VoskRecognizer` 65-90; de-branch
  `make_recognizer_factory` 177-220)
- Modify: `core/commands.py` (delete `_DEFAULT_VOSK_MODEL_DIR/_NAME` 585-586,
  `get_recognizer_backend` 593-600, `get_vosk_model_path` 704-730;
  emit-defaults: drop `vosk_model` + `recognizer_backend` keys 1517-1518)
- Modify: `core/settings/helpers.py:103` (delete dead `VOSK_MODELS_DIR`)
- Modify: `voice_commander.py:207-218` (drop backend print, fix error msg)
- Modify: `requirements.txt:1` (drop `vosk>=0.3.45`)
- Modify: `install.sh` (constants ~41-46, `install_vosk_model()` 277-304,
  call site ~494, header comment :13)
- Modify: `ARCHITECTURE.md` (§Recognizer seam 64-107 prose, §Vosk
  normalization split 108-129 retitle/reword, §Precision-vs-recall :139
  one word, §single-instance 373-395 + §--activate 397-429 prose, plus
  grep sweep)
- Tests: `tests/test_recognizer.py`, `tests/test_whisper_wiring.py`,
  `tests/test_install_identity.py:43`

**Interfaces:** `make_recognizer_factory()` keeps its name/signature,
always builds whisper. `get_recognizer_backend()` ceases to exist —
nothing may call it afterward (only callers: recognizer.py:188,
voice_commander.py:211).

- [ ] **Step 1: Tests first.**
  - `tests/test_recognizer.py`: delete `FakeKaldiRecognizer` (23-51),
    `fake_vosk` fixture (53-64), and the 7 vosk tests (73-129). KEEP
    `test_sample_rate_is_hoisted_to_one_place`.
  - `tests/test_whisper_wiring.py`: delete `test_backend_defaults_to_vosk`
    (:35), `test_backend_reads_config_key` (:40),
    `test_unknown_backend_falls_back_to_vosk` (:45),
    `test_factory_vosk_backend_loads_model_once_and_wraps` (208-227).
    In `test_emit_defaults_includes_whisper_keys` (:84): replace the
    `config["recognizer_backend"] == "vosk"` assertion (:93) with
    `assert "recognizer_backend" not in config` and
    `assert "vosk_model" not in config`.
  - `tests/test_install_identity.py:43`: delete the
    `_DEFAULT_VOSK_MODEL_DIR` assertion line.
  - KEEP `tests/test_listener.py:346` `test_listener_module_does_not_import_vosk`
    for now (still passes; the seam guard idea survives) and
    `test_vosk_mode_still_sleeps_after_match` :387 — rename it
    `test_streaming_mode_still_sleeps_after_match` (it tests the
    `per_segment_finals=False` seam default, which stays).

- [ ] **Step 2: Run the suite, confirm the edited tests fail** for the
  right reason (emit-defaults still ships the keys):
  `python -m pytest tests/test_whisper_wiring.py -q` → FAIL.

- [ ] **Step 3: `core/recognizer.py`** — delete `VoskRecognizer` (65-90)
  and rewrite the factory tail. Post-state:

```python
def make_recognizer_factory():
    """Build the whisper recognizer factory.

    Called once at app startup (voice_commander.py). Heavy one-time setup
    happens HERE -- the whisper-server unit spin-up -- while the returned
    zero-arg factory only does cheap per-listener-run work, so mic
    restarts stay fast.
    """
    import core.commands as commands
    import core.vad as vad
    import core.whisper_client as whisper_client
    import core.whisper_server as whisper_server

    # Bring the transcription server up now (unit restart if config
    # changed). Failure is non-fatal: the listener surfaces unreachable-
    # server errors per segment, and systemd keeps retrying the unit.
    whisper_server.ensure_server()

    url = f"http://127.0.0.1:{commands.get_whisper_server_port()}/inference"
    vad_model = commands.get_vad_model_path()
    tail_ms = commands.get_whisper_vad_tail_ms()

    def factory():
        return WhisperRecognizer(
            vad=vad.SileroVAD(vad_model),
            transcribe=lambda pcm: whisper_client.transcribe_pcm(pcm, url),
            tail_ms=tail_ms,
        )

    return factory
```

  Also update module-level prose: `json` import stays only if still used
  (check — `VoskRecognizer` was the only `json.loads` user; if so, drop the
  import), and reword docstring/comment mentions of Vosk (module docstring,
  `per_segment_finals` comments, "Vosk never does this" at listener is
  Step 6).

- [ ] **Step 4: `core/commands.py`** — delete `_DEFAULT_VOSK_MODEL_DIR`,
  `_DEFAULT_VOSK_MODEL_NAME` (585-586), `get_recognizer_backend()`
  (593-600), `get_vosk_model_path()` (704-730). Reword the `_MODELS_DIR`
  comment (588-589) to drop "unlike the legacy vosk-model/ dir". In the
  emit-defaults dict, delete the `"vosk_model"` and `"recognizer_backend"`
  lines (1517-1518).

- [ ] **Step 5: peripherals** —
  - `core/settings/helpers.py:103`: delete `VOSK_MODELS_DIR` (referenced by
    nothing).
  - `voice_commander.py`: delete `backend = commands.get_recognizer_backend()`
    and the print (:211-212); error message at :216 becomes
    `f"{_TAG} ERROR: Failed to set up whisper backend: {e}"`. Reword the
    comments at :172-174 ("costly config and Vosk model loads") and
    :207-210.
  - `requirements.txt`: delete line 1 (`vosk>=0.3.45`).
  - `install.sh`: delete `VOSK_DIR`/`VOSK_MODEL_NAME`/`VOSK_MODEL_URL`
    (~41-46), the whole `install_vosk_model()` function (277-304), its
    call in `main()` (~494), and the header-comment mention (:13).

- [ ] **Step 6: prose sweep** — `grep -rni vosk core/ voice_commander.py
  uninstall.sh vc-whisper-server.service.in tests/` and fix every LIE
  (claims about current behavior); leave true historical notes ("the
  Vosk→Whisper migration") where they still read correctly. Known sites:
  `core/aliases.py:28,32`, `core/matcher.py:33`, `core/vad.py:14-15`,
  `core/paths.py:9-10`, `core/wake.py:6,10`, `core/overrides.py` (33-86),
  `core/listener.py` (docstring :268 "Vosk model", :312 "(vosk)",
  :524 "Vosk never does this", :556 endpointer note), `core/actions/*`,
  `core/settings/dialog.py:8` (Overrides blurb), `uninstall.sh:9`,
  `vc-whisper-server.service.in:14`, `test_matcher.py` comment/test-name.
  Comment-only edits — behavior identical.

- [ ] **Step 7: ARCHITECTURE.md (same commit)** —
  - §"Recognizer seam" (64-107): strip Vosk clauses ("Vosk, being local,
    never emits it", "Vosk never returns None", "(Vosk JSON envelopes...)",
    "import vosk inside VoskRecognizer.__init__" example, "the Vosk model
    stays loaded" → "heavy engine state stays loaded"). The seam contract
    itself (events, factory, SAMPLE_RATE, injected collaborators) is
    UNCHANGED. The audio-wake sentence at 73-75 gets rewritten in Task 3
    (leave a clean whisper-outage justification: the "error" event exists
    so an outage warns loudly instead of dying silently).
  - §"Vosk normalization split" (108-129): retitle to
    "Normalization split: hallucination filter vs user overrides"; reword
    "Vosk transcripts" → "transcripts". Mechanism text unchanged.
  - §"Precision-vs-recall" (:139): "plausible Vosk garbles" → "plausible
    engine garbles".
  - §single-instance (375, 392) + §--activate (400): "Vosk model
    load"/"Vosk import" → "model load"/"engine import". 
  - Then `grep -ni vosk ARCHITECTURE.md` → remaining hits (281, 465, 600,
    906-954 partially handled in Task 3, 1021) get the same lie-vs-history
    treatment.

- [ ] **Step 8: Full gauntlet** → green.

- [ ] **Step 9: COMMIT GATE** — summary to Tyler; on yes, commit:
  `feat: remove the Vosk recognizer arm -- whisper is the only backend`.

---

### Task 3: Audio-wake arm (the big one)

**Files:**
- Delete: `core/wakeword.py`, `core/wake_dump.py`, `tests/test_wakeword.py`,
  `tests/test_wake_dump.py`, `wakewords/computer_v2.onnx`,
  `wakewords/ATTRIBUTION.md`, `benchmark/soak_wakeword.py`,
  `benchmark/train_v2_verifier.py`
- Modify: `.gitignore:46-54` (drop the two `!benchmark/...` negations;
  keep `benchmark/*` ignore)
- Modify: `core/listener.py` (surgery below), `core/wake.py` (delete
  `fuzzy_check` 74-96 + docstring reword), `core/commands.py` (7 getters +
  7 emit-defaults keys), `voice_commander.py` (import/factory/args),
  `requirements.txt` (lines 9-23 → oww block gone), `install.sh`
  (--no-deps hack 229-233, `install_wakeword_models()` 337-366 + call ~496)
- Modify: `tests/test_listener.py` (blocks below), `tests/test_wake.py`
  (127-167)
- Modify: `ARCHITECTURE.md` §904-1009 + recognizer-seam sentence 73-75 +
  `core/recognizer.py:166-168` comment

**Interfaces:** `run_listener()` loses the `wake_engine_factory` kwarg
(callers: voice_commander.py:161, tests/test_listener.py:133).
`WakeWordDetector` keeps `check`/`is_wake_only`; loses `fuzzy_check`.
`core/wake.py` stays stdlib-only.

- [ ] **Step 1: Delete whole files** — `git rm core/wakeword.py
  core/wake_dump.py tests/test_wakeword.py tests/test_wake_dump.py
  wakewords/computer_v2.onnx wakewords/ATTRIBUTION.md
  benchmark/soak_wakeword.py benchmark/train_v2_verifier.py` (gated: this
  is a git write — fold approval into this task's commit gate; use plain
  `rm` + `git add -A` at commit time if preferred). Remove the two
  `.gitignore` negation lines (`!benchmark/train_v2_verifier.py`,
  `!benchmark/soak_wakeword.py`). Local untracked HA material
  (COLAB notes, oww-venv, corpora) is ignored by `benchmark/*` and
  survives on disk; git history keeps the deleted scripts.

- [ ] **Step 2: `core/listener.py` surgery** (top to bottom):
  - :43 delete `from core import wake_dump`.
  - :257 delete `wake_engine_factory=None` param; delete its docstring
    paragraph (269-274).
  - 316-340: delete the whole engine-construction block (`wake_engine =
    None` through the verifier toast) including the s31 comment.
  - 344-358: delete the wake-dump block (comment + `wake_dump_dir` +
    `wake_ring`) and the confirm block (comment + `confirm_threshold` +
    `audio_wake_unconfirmed`).
  - 401-408: in the window-expiry chain, delete the
    `elif audio_wake_unconfirmed:` rung entirely — `if matched_since_wake:`
    ... `else:` (No match toast) remain.
  - 459-519: delete the entire `# -- Audio wake engine (openWakeWord) --`
    block.
  - :652, :658, :667, :702 delete the `audio_wake_unconfirmed = False`
    lines (and at :649-652 trim the comment to just "The wake word,
    transcribed -- keep listening for the command."). 
  - 680-690: delete the whole `elif audio_wake_unconfirmed and not
    detector.fuzzy_check(...)` rung — the final `else:` (one-shot No
    match, 691-706) becomes the direct alternative to the `try_match` if.
  - 527-541 + 360-365: KEEP the transcription-down machinery; reword its
    comments to stand alone (drop "separate from the audio-wake
    silent-revert on purpose" framing; the point is: warn loudly once per
    outage so a dead whisper-server never looks like a dead app).
- [ ] **Step 3: `core/wake.py`** — delete `fuzzy_check` (74-96); reword the
  `BAKED_IN_WAKE_WORDS` docstring (17-20): "computer" is baked in because
  it is THE shipped wake word the UI copy and muscle memory assume —
  drop the "shipped openWakeWord model (computer_v2)" justification.
  Module stays stdlib-only (`re`, `difflib` — difflib now unused? CHECK:
  if `fuzzy_check` was the only `difflib` user, drop the import).

- [ ] **Step 4: `core/commands.py`** — delete `get_wake_engine` (629-637),
  `get_wake_model_path` (640-644), `get_wake_threshold` (647-648),
  `get_wake_vad_threshold` (651-660), `get_wake_verifier_path` (663-675),
  `get_wake_audio_dump_dir` (678-689), `get_wake_confirm` (692-701).
  In emit-defaults delete the 7 lines: `wake_engine`, `wake_model`,
  `wake_threshold`, `wake_vad_threshold`, `wake_verifier`,
  `wake_audio_dump`, `wake_confirm` (1522-1528).

- [ ] **Step 5: `voice_commander.py`** — delete the import (:56), the
  factory setup block (220-227), `wake_engine_factory` from
  `_listener_thread` signature (:130), the `run_listener` kwarg (:161),
  and the thread `args` tuple entry (:236). Post-state args:
  `args=(recognizer_factory, detector, context, GUI_ENV, state_queue,
  command_queue)`.

- [ ] **Step 6: Tests** —
  - `tests/test_listener.py`: delete the audio-wake block 463-720 (both
    fake engine classes + 12 tests incl. dump tests) and the confirm tests
    725-805 (`test_audio_phantom_*`, `test_audio_wake_with_*`,
    `test_wake_confirm_disabled_restores_nagging`). KEEP
    `test_transcription_failure_warns_loudly_once` (:806),
    `test_transcription_recovery_rearms_the_warning` (:823),
    `test_text_wake_miss_still_nags` (:837). In the harness, drop the
    `wake_engine=None` param (:40) and the factory line (:133).
  - `tests/test_wake.py`: delete the fuzzy_check block (127-167).
  - Files already deleted in Step 1 cover the rest.

- [ ] **Step 7: `requirements.txt`** — post-state (complete file):

```
PySide6>=6.6.0
# Whisper backend: Silero VAD runs on onnxruntime (numpy comes with it,
# pinned here anyway since core/vad.py imports it directly). The Whisper
# transcription itself runs in whisper-server (system package whisper-cpp),
# not in Python.
onnxruntime>=1.20.0
numpy>=1.26.0
```

- [ ] **Step 8: `install.sh`** — delete the oww pip line + its comment
  (229-233), the whole `install_wakeword_models()` (337-366), and its call
  in `main()` (~496). The venv `rm -rf` + rebuild (:225) purges the
  packages on next deploy automatically.

- [ ] **Step 9: ARCHITECTURE.md (same commit)** — §"Wake is acknowledged on
  partials..." (904-1009): delete the sub-blocks "Audio-wake text
  confirmation (s34)" (926-943), "Audio wake engine (s31)" (955-967),
  "Speaker verifier (s34)" (968-987), the exception-pointer tail at
  924-926, and the eight oww bullets in **Don't** (993-1009, keeping the
  generic 990-993). Keep: wake-on-partials, inactivity window, wake-only
  swallowed / one-shot no-match, per-segment chains, Why. Fix the
  recognizer-seam sentence (73-75): the `"error"` event exists so the
  listener warns LOUDLY that transcription is down (an outage must never
  look like a dead app — the ggml-0.20 breakage, 2026-08-17). Same fix to
  the comment in `core/recognizer.py:164-168`. Then
  `grep -ni "openwakeword\|oww\|wake_engine\|wake_confirm\|verifier" ARCHITECTURE.md`
  and sweep the stragglers with the lie-vs-history rule.

- [ ] **Step 10: Full gauntlet** → green. Also
  `grep -rn "wakeword\|wake_dump\|wake_confirm\|fuzzy_check" core/ voice_commander.py tests/`
  → should return ONLY `core/wake.py`-adjacent text-wake identifiers
  (`WakeWordDetector` etc.) and no dead references.

- [ ] **Step 11: COMMIT GATE** — summary to Tyler; on yes, commit:
  `feat: remove the openWakeWord audio-wake arm -- text wake is the only wake path`.

---

### Task 4: README surgical pass + CHANGELOG

**Files:** Modify `README.md` (vosk/oww claims at ~5, 53, 172, 186, 244,
253, 276, 280), `CHANGELOG.md` (new Removed section)

- [ ] **Step 1:** Read README.md in full; fix ONLY factual lies (Vosk as
  the engine, model download claims, wake-engine claims, dependency
  claims). No restructuring — the rewrite is its own 2.0 session.
- [ ] **Step 2:** CHANGELOG.md: add an Unreleased "Removed" block: Vosk
  backend (+ `vosk_model`/`recognizer_backend` keys), openWakeWord audio
  wake (+ the 7 wake keys, wake-dump diagnostics, vendored model, oww-only
  deps), celery_man from shipped defaults (existing configs unaffected).
- [ ] **Step 3:** Gauntlet (docs don't break tests, but cheap insurance) →
  COMMIT GATE: `docs: scrub Vosk/openWakeWord claims from README; changelog`.
  (README standing rule allows auto-push of README-only edits, but this
  commit is mixed — push stays gated with the rest.)

---

### Task 5: Deploy + live verification (GATED) + HANDOFF

- [ ] **Step 1: DEPLOY GATE** — ask Tyler explicitly. On yes:
  `./install.sh` (venv rebuild drops vosk/openwakeword/scipy/sklearn/tqdm/
  requests; wake-dump keep=500 divergence disappears because the file is
  gone).
- [ ] **Step 2: Byte-verify** deployed app dir against repo (diff -r of
  the copied source set, excluding venv/__pycache__).
- [ ] **Step 3:** `systemctl --user restart voice-commander.service`, then
  `journalctl --user -u voice-commander.service -n 50` — expect: no
  "Recognizer backend:" line, no "Audio wake engine" line, no tracebacks,
  "Listening on:" present.
- [ ] **Step 4: Live smoke (Tyler):** say "computer" → Listening toast;
  "computer open github" one-breath → command fires; a nonsense phrase →
  one "No match" toast (one-shot preserved). Media auto-pause still works
  (wake-window seam untouched).
- [ ] **Step 5:** Update HANDOFF.md (retire-first): scrub DONE, new
  invariant index state, stale-key note (Tyler's live config still carries
  wake_*/vosk keys + celery row — harmless by design), next rocks
  (README rewrite / 2.0 tag / DEFAULT_OVERRIDES rebuild). Leftover runtime
  dirs `DATA_DIR/wakewords/`, `DATA_DIR/wake_dumps/`, `DATA_DIR/vosk-model/`
  can be manually deleted whenever — flag to Tyler, don't auto-delete.
- [ ] **Step 6:** Offer (don't assume) a push of the accumulated commits.

## Verification summary

- Per-commit: the gauntlet (pytest + compileall + emit-defaults).
- Expected test-count delta: −~55 (22 wakeword + 9 wake_dump + 7 vosk
  recognizer + 3-4 wiring + 12 listener audio + 5 confirm + 7 fuzzy −
  the few flips/renames). Suite must be green at every commit, not just
  the end.
- End-to-end: Task 5 journal check + Tyler's live smoke.
- Post-deploy: emit-defaults from the DEPLOYED venv must show no
  vosk/wake keys: `~/.local/share/voice-commander/venv/bin/python -m
  core.commands --emit-defaults` (run from the deployed app dir).

## Explicitly out of scope

- Any config prune / migration for existing users' stale keys.
- README full rewrite, 2.0 tag, Hyprland port.
- Pushing to origin (separate gated decision; main is already 1 ahead).
- `per_segment_finals` seam simplification (harness depends on it).
- Deleting local untracked HA material under benchmark/.
